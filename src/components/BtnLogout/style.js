import styled from 'styled-components'

export const Content = styled.div`
    img {
        width: 50px;
        transition: 0.3s;

        &:hover {
            cursor: pointer;
            scale: 1.1;
        }
    }
`